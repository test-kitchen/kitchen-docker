machine:
  services:
    - docker

  # Version of ruby to use
  ruby:
    version:
     2.1.5
dependencies:
  bundler:
    without: [ development ]
  pre:
    - rvm install 2.0.0-p598
    - rvm install 2.2.0

test:
  override:
    - bundle exec rake
    - echo bundle exec kitchen test
